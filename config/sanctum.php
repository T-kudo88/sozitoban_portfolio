<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Stateful Domains
    |--------------------------------------------------------------------------
    |
    | フロントエンドとAPIの認証に必要なドメインを設定します。
    | ここに `127.0.0.1:8003` を追加することで、SPAの認証が機能するようになります。
    |
    */

    // 'stateful' => explode(',', env('SANCTUM_STATEFUL_DOMAINS', '127.0.0.1:8003,localhost,localhost:3000,127.0.0.1,127.0.0.1:8000,::1')),
    'stateful' => explode(',', env('SANCTUM_STATEFUL_DOMAINS', 'localhost:5173')),

    /*
    |--------------------------------------------------------------------------
    | Sanctum Guards
    |--------------------------------------------------------------------------
    |
    | Sanctum がリクエストの認証を試みる際にチェックするガードを定義します。
    |
    */

    'guard' => ['web'],

    /*
    |--------------------------------------------------------------------------
    | Expiration Minutes
    |--------------------------------------------------------------------------
    |
    | トークンの有効期限を指定します。null にすると無期限になります。
    |
    */

    'expiration' => null,

    /*
    |--------------------------------------------------------------------------
    | Token Prefix
    |--------------------------------------------------------------------------
    |
    | GitHub のセキュリティスキャンを回避するために、トークンのプレフィックスを設定できます。
    |
    */

    'token_prefix' => env('SANCTUM_TOKEN_PREFIX', ''),

    /*
    |--------------------------------------------------------------------------
    | Sanctum Middleware
    |--------------------------------------------------------------------------
    |
    | CSRFトークンの検証を正しく機能させるために `verify_csrf_token` を指定。
    |
    */

    'middleware' => [
        'authenticate_session' => Laravel\Sanctum\Http\Middleware\AuthenticateSession::class,
        'encrypt_cookies' => Illuminate\Cookie\Middleware\EncryptCookies::class,
        'verify_csrf_token' => App\Http\Middleware\VerifyCsrfToken::class,
    ],

];
